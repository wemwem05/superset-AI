---
phase: 02-reliability-performance
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - .env.example
autonomous: true
gap_closure: true
requirements: [REL-01]

must_haves:
  truths:
    - "Operators have a template file showing all required environment variables for the deployment"
    - "S3 backup credentials (S3_BUCKET, S3_REGION, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY) are documented in .env.example"
    - "PostgreSQL backup sidecar can be configured by copying .env.example to .env and filling in values"
  artifacts:
    - path: ".env.example"
      provides: "Environment variable template for all services including S3 backup credentials"
      contains: "S3_BUCKET"
  key_links:
    - from: "docker-compose.yml postgres-backup environment"
      to: ".env.example S3 variables"
      via: "${S3_*} variable references matched by .env.example placeholders"
      pattern: "S3_BUCKET"
---

<objective>
Create the missing .env.example file that provides operators with a complete environment variable template, closing the verification gap that left REL-01 partially satisfied.

Purpose: The postgres-backup sidecar in docker-compose.yml references ${S3_BUCKET}, ${S3_REGION}, ${S3_ACCESS_KEY_ID}, and ${S3_SECRET_ACCESS_KEY}, but no .env.example exists to document these variables for operators. Without it, the backup sidecar fails silently and operators have no way to discover the required configuration.

Output: .env.example file at project root with all environment variable placeholders.
</objective>

<execution_context>
@/Users/alimuratbekov/work/superset/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alimuratbekov/work/superset/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reliability-performance/02-01-SUMMARY.md
@.planning/phases/02-reliability-performance/02-VERIFICATION.md

<interfaces>
<!-- Variables referenced in docker-compose.yml that .env.example must document -->

From docker-compose.yml postgres-backup service (lines 181-190):
```yaml
S3_REGION: ${S3_REGION}
S3_BUCKET: ${S3_BUCKET}
S3_PREFIX: superset-backups
S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID}
S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY}
# S3_ENDPOINT: ${S3_ENDPOINT}
POSTGRES_HOST: postgres
POSTGRES_DATABASE: ${POSTGRES_DB}
POSTGRES_USER: ${POSTGRES_USER}
POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
```

From docker-compose.yml superset service (line 92):
```yaml
- "${SUPERSET_PORT:-8088}:8088"
```

From docker/superset_config.py (lines 36-37):
```python
REDIS_HOST = os.environ.get("REDIS_HOST", "redis")
REDIS_PORT = int(os.environ.get("REDIS_PORT", 6379))
```

From docker-compose.yml env_file directive:
```yaml
env_file: .env
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with all environment variable placeholders</name>
  <files>.env.example</files>
  <read_first>
    - docker-compose.yml (lines 80-200 — verify all ${VAR} references that need placeholders)
    - docker/superset_config.py (lines 30-40 — verify REDIS_HOST, REDIS_PORT env var defaults)
    - .planning/phases/02-reliability-performance/02-VERIFICATION.md (gap description with exact missing items)
  </read_first>
  <action>
Create a new file `.env.example` at the project root with the following exact content. This file documents every environment variable consumed by docker-compose.yml and superset_config.py, organized by service. The content matches what was specified in the original 02-01-PLAN.md Task 2 action block but was never written to disk.

```
# =============================================================================
# Superset Environment Variables
# =============================================================================
# Copy this file to .env and fill in actual values.
# NEVER commit .env to version control.

# --- PostgreSQL ---
POSTGRES_USER=superset
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=superset
DATABASE_HOST=postgres
DATABASE_PORT=5432

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Celery ---
# Broker URL used by celery workers and healthcheck (celery inspect ping).
# Must match CeleryConfig.broker_url in superset_config.py (Redis DB 4).
CELERY_BROKER_URL=redis://redis:6379/4

# --- Superset ---
# Generate once: openssl rand -base64 42
# NEVER rotate after first deployment — it encrypts saved DB credentials.
SUPERSET_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL
SUPERSET_PORT=8088

# --- S3 Backup (REL-01) ---
# Required for automated PostgreSQL backups to S3-compatible storage.
# Create a bucket and IAM credentials with PutObject/DeleteObject permissions.
S3_REGION=us-east-1
S3_BUCKET=my-superset-backups
S3_ACCESS_KEY_ID=CHANGE_ME
S3_SECRET_ACCESS_KEY=CHANGE_ME
# Uncomment for non-AWS S3-compatible providers (e.g., Cloudflare R2, MinIO):
# S3_ENDPOINT=https://your-s3-endpoint.example.com
```

Write this file using the Write tool. Do NOT modify any other files — docker-compose.yml, superset_config.py, and all other artifacts are already verified correct per 02-VERIFICATION.md.
  </action>
  <verify>
    <automated>cd /Users/alimuratbekov/work/superset && test -f .env.example && echo "FILE EXISTS" && grep -q "S3_BUCKET" .env.example && echo "S3_BUCKET present" && grep -q "S3_REGION" .env.example && echo "S3_REGION present" && grep -q "S3_ACCESS_KEY_ID" .env.example && echo "S3_ACCESS_KEY_ID present" && grep -q "S3_SECRET_ACCESS_KEY" .env.example && echo "S3_SECRET_ACCESS_KEY present" && grep -q "S3_ENDPOINT" .env.example && echo "S3_ENDPOINT present (commented)" && grep -q "CELERY_BROKER_URL" .env.example && echo "CELERY_BROKER_URL present" && grep -q "SUPERSET_SECRET_KEY" .env.example && echo "SUPERSET_SECRET_KEY present" && grep -q "POSTGRES_USER" .env.example && echo "POSTGRES_USER present" && grep -q "POSTGRES_PASSWORD" .env.example && echo "POSTGRES_PASSWORD present" && grep -q "REDIS_HOST" .env.example && echo "REDIS_HOST present" && grep -q "openssl rand" .env.example && echo "openssl generation comment present" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example file exists at project root `/Users/alimuratbekov/work/superset/.env.example`
    - Contains `POSTGRES_USER=superset` (default value)
    - Contains `POSTGRES_PASSWORD=CHANGE_ME` (placeholder)
    - Contains `POSTGRES_DB=superset` (default value)
    - Contains `DATABASE_HOST=postgres` (Docker service name)
    - Contains `DATABASE_PORT=5432`
    - Contains `REDIS_HOST=redis` (Docker service name)
    - Contains `REDIS_PORT=6379`
    - Contains `CELERY_BROKER_URL=redis://redis:6379/4` (matches superset_config.py Redis DB 4)
    - Contains `SUPERSET_SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL` with comment about `openssl rand -base64 42`
    - Contains `SUPERSET_PORT=8088`
    - Contains `S3_REGION=us-east-1` (example value)
    - Contains `S3_BUCKET=my-superset-backups` (example value)
    - Contains `S3_ACCESS_KEY_ID=CHANGE_ME`
    - Contains `S3_SECRET_ACCESS_KEY=CHANGE_ME`
    - Contains commented line `# S3_ENDPOINT=https://your-s3-endpoint.example.com`
    - File header includes instruction to copy to .env and never commit .env
  </acceptance_criteria>
  <done>.env.example exists at project root with all PostgreSQL, Redis, Celery, Superset, and S3 credential placeholders. The verification gap for REL-01 is closed — operators now have a documented template for configuring the backup sidecar's S3 credentials.</done>
</task>

</tasks>

<verification>
- `test -f .env.example` exits 0 (file exists)
- `grep -c 'S3_' .env.example` returns at least 5 (S3_REGION, S3_BUCKET, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY, S3_ENDPOINT)
- `grep -c 'CHANGE_ME' .env.example` returns at least 3 (POSTGRES_PASSWORD, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY)
- `grep 'CELERY_BROKER_URL=redis://redis:6379/4' .env.example` matches
- `grep 'openssl rand' .env.example` matches (generation instruction for SUPERSET_SECRET_KEY)
- docker-compose.yml `env_file: .env` directive means variables from .env.example (once copied to .env) will be injected into all services via the x-superset-common anchor
</verification>

<success_criteria>
REL-01 gap closed: .env.example provides the S3 credential template that operators need to configure the postgres-backup sidecar. The key_link from docker-compose.yml ${S3_*} references to .env.example is now wired. Phase 02 verification score moves from 9/10 to 10/10.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-performance/02-02-SUMMARY.md`
</output>
